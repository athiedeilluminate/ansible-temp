---
- name: test playbook
  hosts: all
  tasks:
  - name: install some packages
    apt:
      name:
        - vim
        - git
        - neofetch
  - name: run wall
    command:
      cmd: wall -n "Hello from Ansible"
